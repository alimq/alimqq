{% layout default_layout %}
{% block content %}
    {{ 'hero-cta.css' | asset_url | stylesheet_tag }}
    {{ 'hero.css' | asset_url | stylesheet_tag }}
    {{ 'contacts.css' | asset_url | stylesheet_tag }}
    <div class="hero">
        <div class="container">
            <div class="cta-div">
                <p class="cta-p">Full portfolio</p>
            </div>
        </div>
    </div>
{% endblock %}