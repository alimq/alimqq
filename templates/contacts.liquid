{% layout default_layout %}
{% block content %}
    {{ 'css/hero-cta.css' | asset_url | stylesheet_tag }}
    {{ 'css/hero.css' | asset_url | stylesheet_tag }}
    {{ 'css/contacts.css' | asset_url | stylesheet_tag }}
    <div class="hero">
        <div class="container">
            <div class="cta-div">
                <p class="cta-p">Let's</p>  
                <a target="_blank"
                 href="https://www.linkedin.com/in/alim-imanmalik/">
                <button class="send-btn">
                connect on<img class="btn-pic" src="{{ 'linkedin.svg' | asset_url }}">
                </button></a><p class="cta-p">!</p>
            </div>
            <p class="cta-p cta-paragraph">In case our networks can help each other</p>
        </div>
    </div>
{% endblock %}